version: '2'
services:
  gateway:
    image: docker-gateway-test
    user: '1000001'
    ports:
      - "8080:8080"
      - "8090:8090"
    env_file: .env
  test:
    image: docker-gateway-test
    depends_on:
      - gateway
    entrypoint: ""
